public int add(int [] a ) {

int val = 0 ; 
for(int i ; i < a.size() ; i++){
val = val + a[i];
}

return val 

}
public int mul(int [] a ) {

int val = 0 ; 
for(int i ; i < a.size() ; i++){
val = val * a[i];
}

return val 

}
